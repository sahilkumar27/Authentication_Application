#development version of configuration

server:
  port: 8083

# database connection

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/auth_app_dev?useSSL=false&serverTimezone=UTC
    username: root
    password: root123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect # Updated line